/*
 * 
 * TODO: Automatically attach events to quest objectives, so that the quest system can be more modular and not 
 * require hardcoding of events in the QuestEvents script. This would allow for easier addition of new quests and objectives without needing to modify the QuestEvents script each time.
 * 
 * FOR NPC INTERACTIONS:
 * Find out how to automatically attach events to an npc. After pressing virtual void button, it should record its npc name to be sent to QuestEvents
 * 
 * 
 */